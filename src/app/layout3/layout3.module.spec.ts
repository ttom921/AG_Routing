import { Layout3Module } from './layout3.module';

describe('Layout3Module', () => {
  let layout3Module: Layout3Module;

  beforeEach(() => {
    layout3Module = new Layout3Module();
  });

  it('should create an instance', () => {
    expect(layout3Module).toBeTruthy();
  });
});
